spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:~/Documents/spring-boot-in-action/spring-boot-in-action/testdb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2

application:
  name: user-service
  version: 1.0.0
  env: dev
  title: 'From "spring-book-in-action"'
